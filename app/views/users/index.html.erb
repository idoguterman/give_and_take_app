<% title "Users" %>
<%= form_tag users_path, :method => :get, :remote => true , :id => "users_search", :class => "search-form" do %>
  <p>
    <%= text_field_tag :search, params[:search] %>
    <%= submit_tag "Search", :name => nil %>
  <% end %>

  <div id="users"><%= render :partial => 'users/users' %></div>
  <p><%= link_to "New User", new_user_path %></p>
